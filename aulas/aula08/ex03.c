// aula 8 ex 3 //
#include <stdio.h>

void regressiva (int n) {
    if (n < 1) {
        printf("Vai!");
        return;
    }
        printf("%d, ", n);
        regressva(n - 1);
}

void progressiva (int n) {
    if (n > 1) {
        
    }
}